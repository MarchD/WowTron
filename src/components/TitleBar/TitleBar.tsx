const TitleBar = () => {
  // TODO will be implemented with customization
  // const closeApp = () => {
  //   ipcRenderer.invoke('close-app');
  // };

  // const minimizeApp = () => {
  //   ipcRenderer.send('minimize-app');
  // };

  // const maximizeRestoreApp = () => {
  //   ipcRenderer.invoke('maximize-restore-app');
  // };

  return (
    <div>
      Title Bar
    </div>
  );
};

export default TitleBar;
